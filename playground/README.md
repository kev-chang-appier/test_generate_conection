# Playground

This folder holds **test artifacts** — files generated by tasks while experimenting
with the repo's tooling (e.g. the `test-ds-prototype-local` skill).

Nothing here is a reference, spec, or production asset. Each file is simply the
**result of a test run** and can be regenerated or removed at any time.

## Contents

| File | Description |
|---|---|
| `zalo-connector.html` | Prototype HTML output produced by a prior test task. |
