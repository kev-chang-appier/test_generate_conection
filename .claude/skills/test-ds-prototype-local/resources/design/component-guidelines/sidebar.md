# Sidebar Usage Guidelines

> **Status: pending rewrite.** The previous version of this guide was found to be
> inaccurate and has been removed to prevent it from misleading implementation.
>
> For the actual navigation shell — Sidebar, TopBar, and PageHeader structure — refer to
> the product shells (`aiqua-shell.html` for AIQUA/AQ, `botbonnie-shell.html` for
> BotBonnie/BB) in [`../Design-Patterns/`](../Design-Patterns/), which are the source of
> truth for page-level navigation. Do not rely on this document until it is rewritten.

<!-- TODO: Rewrite against the DS3 `*sidebar 3` component — anatomy, states, and tokens. -->
